[cmake-quickstart]
command = cp $(VIM_INIHOME)/tasks-template/cmake-cpp/* $(VIM_ROOT)/ && sed -i 's/$PROJ_NAME/$(?project name)/g' $(VIM_ROOT)/CMakeLists.txt

output=terminal


[cmake-build]
cwd = $(VIM_ROOT)
command = mkdir -p build && cmake ./ -B build
output=terminal
